name: Build the Docker image

run:
  docker version
  docker login --username=${{ secrets.DOCKER_USERNAME }} --password=${{ secrets.DOCKER_PASSWORD }} ghcr.io
  cd walletbackend
  docker build  --file Dockerfile --tag ghcr.io/maopenweis/api:v1.0
  dockerr push ghcr.io/maopenweeis/api:v1.0
